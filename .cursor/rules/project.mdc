---
description: Project-specific rules (auto-generated by AI Project Kit)
alwaysApply: true
---

# Project: velo-office360-api-ya-clean

**Path:** `C:\Users\gosha\OneDrive\Документы\CR\YALINUX360\velo-office360-api-ya-clean`

## Stack

Node.js (package.json - parse error), Tauri (Rust)

## Key directories

- `.ai/`
- `.claude/`
- `.cursor/`
- `.github/`
- `assets/`
- `docs/`
- `landing/`
- `public/`
- `scripts/`
- `src/`
- `src-tauri/`

## Project rules

- **Graphify-first** for non-trivial existing-code work — canonical: `.agents/skills/office360-project/SKILL.md` § Graphify-first; thin rule: `30-graphify-first.mdc`.

## App type

- **Tauri / Yandex 360** desktop app (`office360`).

## Do not touch

- OAuth secrets, refresh tokens, client secrets.
- Do not log tokens or credentials.

## Checks

- Before edits: read `package.json` scripts.
- Rust/Tauri checks (`cargo check`, `npm run tauri build`) — only if environment is ready.
- Default: `npm run lint`, `npm test`, `npm run build` when applicable.

## Notes

- Nested project inside `YALINUX360/`.
- Prefer targeted changes; avoid breaking OAuth flow.

## Checks

```bash
npm run test
```

```bash
npm run build
```

- cargo check in src-tauri/ - only if Rust toolchain ready

## References

- `.ai/PROJECT_CONTEXT.md`
- `.ai/DEPLOY_RUNBOOK.md`
- `.ai/SECRETS_POLICY.md`
- `.ai/CURRENT_STATE.md`
